[build-system]
requires = ["setuptools>=42", "pybind11>=2.10"]
build-backend = "setuptools.build_meta"

[project]
name = "kstrie"
version = "0.1.0"
description = "Ordered associative container with string keys and prefix operations — trie/B-tree hybrid"
requires-python = ">=3.8"
