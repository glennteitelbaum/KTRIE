[build-system]
requires = ["setuptools>=42", "pybind11>=2.10"]
build-backend = "setuptools.build_meta"

[project]
name = "ktoken"
version = "0.1.0"
description = "Compact BPE tokenizer — 2.7x faster than tiktoken, 35x less memory"
requires-python = ">=3.8"
